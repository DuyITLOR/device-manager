{
  "name": "device-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n \"WEB,SERVER\" -c \"cyan,green\" \"pnpm --filter=web run dev\" \"pnpm --filter=server run dev\"",
    "dev:web": "pnpm --filter=web run dev",
    "dev:server": "pnpm --filter=server run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  }
  ,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
